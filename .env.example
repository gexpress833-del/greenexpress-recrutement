# En local : PORT=4050. Sur Render : PORT est défini automatiquement — ne pas le surcharger.
PORT=4050
# Render : créez une base PostgreSQL et liez-la au service Web ; DATABASE_URL est fournie automatiquement.
# En local, soit une URL complète :
# DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:5432/cv_greenexpress
# soit les variables séparées (sans DATABASE_URL) :
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGPASSWORD=votre_mot_de_passe
PGDATABASE=cv_greenexpress
JWT_SECRET=generez_une_chaine_longue_aleatoire_min_32_caracteres
ADMIN_EMAIL=votre@email.com
ADMIN_PASSWORD=votre_mot_de_passe_admin
